# Required for AI content generation (audits, fix guidance, outreach drafts)
GEMINI_API_KEY=your_gemini_api_key

# Optional — enables citation tracking, competitor benchmarks, and roadmap features
PEEC_API_KEY=your_peec_api_key

# Optional — frontend API base (default: http://localhost:8000)
# VITE_API_URL=http://localhost:8000
